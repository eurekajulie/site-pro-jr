[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Tools/admin não é parte do site público — só roda localmente.
# Forms são auto-detectados via data-netlify="true" no HTML buildado.
